export declare const CookieHelper: {
    getItem(key: string): string;
    getItemOnce(key: string): string;
    setItem(key: string, val: string): void;
    removeItem(key: string): void;
    exist(key: string): boolean;
    clear(): void;
};
